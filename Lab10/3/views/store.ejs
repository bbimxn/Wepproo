<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Book Store</title>
    <style>
        thead ,th{
            background-color: #007bff !important;
            color: white !important;    
            border-color: #dee2e6;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <h1 class="text-center mb-5" style="color: #2c3e50;">Book Store</h1>
        <table class="table table-striped table-bordered"> <%# ทำให้ในตารางสลับสีกันไป %>
            <thead>
                <tr>
                    <th style="width: 5%;">ID</th>
                    <th style="width: 20%;">Book name</th>
                    <th style="width: 30%;">Description</th>
                    <th style="width: 10%;">Price</th>
                    <th style="width: 10%;">Amount</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach((item)=> { %>
                    <tr>
                        <td><%= item.id %></td> 
                        <td><%= item.name %></td>
                        <td><%= item.description %>
                        <td><%= item.price %></td> 
                        <td><%= item.amount %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>